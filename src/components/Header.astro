---
import { navItems, site } from "../data/site";

const pathname = Astro.url.pathname.replace(/\/$/, "") || "/";

function isActive(href: string) {
  const normalized = href.replace(/\/$/, "") || "/";
  if (normalized === "/") return pathname === "/";
  return pathname === normalized || pathname.startsWith(`${normalized}/`);
}
---

<header class="sticky top-0 z-50 border-b border-forest-600 bg-forest-900">
  <div class="relative mx-auto flex max-w-6xl items-center justify-between gap-4 px-4 py-2 sm:gap-5 sm:px-6 sm:py-2.5 lg:px-8">
    <a
      href="/"
      class="intro-reveal intro-reveal--0 group inline-flex shrink-0 items-center no-underline"
      aria-label={`${site.name} トップへ`}
    >
      <img
        src={site.logo}
        alt={site.name}
        width="320"
        height="72"
        class="h-11 w-auto max-w-[13.5rem] object-contain object-left transition duration-200 group-hover:opacity-90 sm:h-12 sm:max-w-[17rem] md:h-14 md:max-w-[20rem] lg:h-16 lg:max-w-[24rem]"
        decoding="async"
      />
    </a>

    <button
      type="button"
      class="inline-flex h-10 w-10 items-center justify-center rounded-sm border border-forest-600 text-cream-200 md:hidden"
      aria-expanded="false"
      aria-controls="site-nav"
      data-nav-toggle
    >
      <span class="sr-only">メニューを開く</span>
      <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
        <path d="M4 7h16M4 12h16M4 17h16" stroke-linecap="round"></path>
      </svg>
    </button>

    <nav
      id="site-nav"
      class="intro-reveal intro-reveal--1 absolute left-0 right-0 top-full hidden border-b border-forest-600 bg-forest-800 px-4 py-3 shadow-lg md:static md:flex md:border-0 md:bg-forest-900 md:p-0 md:shadow-none"
      data-nav-panel
    >
      <ul class="flex flex-col gap-1.5 md:flex-row md:items-center md:gap-2 lg:gap-2.5">
        {
          navItems.map((item) => (
            <li>
              <a
                href={item.href}
                class:list={[
                  "block rounded-sm px-3 py-1.5 text-sm font-medium transition md:px-3.5 md:py-1.5",
                  isActive(item.href)
                    ? "bg-gold-400/15 text-gold-300"
                    : "text-cream-200 hover:bg-forest-700 hover:text-gold-300",
                ]}
                aria-current={isActive(item.href) ? "page" : undefined}
              >
                {item.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
  </div>
</header>

<script>
  const toggle = document.querySelector("[data-nav-toggle]");
  const panel = document.querySelector("[data-nav-panel]");

  toggle?.addEventListener("click", () => {
    const open = panel?.classList.toggle("hidden") === false;
    toggle.setAttribute("aria-expanded", String(open));
  });
</script>
